node_modules/
.next/
.DS_Store
*.log
dist/
build/
.vercel/
.netlify/

# Env files — ignore everything except the committed template.
# (.env.save in particular holds real secrets and must never be committed.)
.env
.env.*
!.env.example

# Real professional data pulled from Supabase for local calibration — never
# commit real people's career histories. The committed test fixture is
# synthetic (tests/scoring/professionals.snapshot.json); see
# scripts/snapshot-professionals.ts.
tests/scoring/professionals.snapshot.local.json
