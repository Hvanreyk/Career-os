node_modules/
.next/
.DS_Store
*.log
dist/
build/
.vercel/
.netlify/

# Env files — ignore everything except the committed template.
# (.env.save in particular holds real secrets and must never be committed.)
.env
.env.*
!.env.example

# Generated hero masters from gpt-image-2 (~16MB of PNGs). These are the
# intermediate source for scripts/process-hero-assets.mjs; the committed
# AVIF/WebP variants in web/public/hero/ are what the app actually ships.
# Kept out of git for repo size — archive them separately if you need the
# originals, since image generation is not deterministic.
design/hero-src/
