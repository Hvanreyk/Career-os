import { Landing } from '@/components/home/Landing';

/**
 * The approved landing page (previously /v1, "Window" plate).
 *
 * The four superseded hero plates and the four superseded design directions
 * remain recoverable from commits 36ea184 and a2b02c7 respectively.
 */
export default function Home() {
  return <Landing />;
}
